# Dependencies & build output
node_modules/
dist/
release/
out/

# Logs
*.log
npm-debug.log*

# Legacy engine code (unused; kept locally only)
backend/
bin/

# Go engine build artifacts (source lives in go-backend/)
go-backend/k_main.exe
go-backend/knots-go-backend.exe
go-backend/*.exe~

# Engine binaries (build-time artifacts, kept locally)
backend/k_main.exe
backend/k_main.exe~
backend/WinDivert.dll
backend/WinDivert64.sys
backend/build/
backend/*.spec
backend/*.pyc
backend/__pycache__/

# Scratch probe files (LIVE tokens — never commit)
probe*.json
probe*.txt
probe_*.json
NUL.json

# Android build outputs & scratch
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/*.txt
mobile/android/screen.png
mobile/android/start-emulator*.bat
mobile/android/start-download.bat
mobile/android/setup-emulator.bat
mobile/android/run-emulator-test.bat
screenshot.png
*.apk

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.vs/

# Runtime state files (written by the engine bridge, not source)
src/pages/Settings/**/*.json

# Env
.env
.env.local
